import React from 'react'

export default function functionalprops(props) {
    return (
        <div>
            hello {props.name} this is functional props from {props.place}
        </div>
    )
}
